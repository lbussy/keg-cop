// Supports About Page

toggleLoader("off");
var numReq = 0 + numReqPre;
var loaded = 0;

function finishLoad() {
    // Catch page finished event from kegcop_pre.js
    // TODO:  Decide if we need any "on load" processing here.
}
